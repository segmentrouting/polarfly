# Topology Tests

This directory contains test scripts for verifying the correctness of network topologies.

## Scripts

### verify_topology.py
Verifies the polarfly topology by checking:
- Each node has the correct number of connections (7-8)
- Interface numbers are sequential and not duplicated
- All connections follow the polarfly pattern

Usage: 